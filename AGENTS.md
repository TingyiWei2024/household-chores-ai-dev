# Context

- `_docs/plan.md` is the source of truth for approved product behavior.
- `backlog.md` is the source of truth for implementation tasks.
- If they conflict or a product decision is unclear, stop and ask instead of guessing.

# Documents

- `_docs/process.md` - read before implementation; describes how work is organized.

# Rules

- Do not modify `_docs/plan.md` or `backlog.md` unless explicitly asked.
- Do not add features outside the approved MVP.
- Do not add dependencies, commit, or push unless explicitly asked.

# Commands

- Install dependencies: TBD
- Run migrations: TBD
- Run development server: TBD
- Run tests: `uv run python manage.py test`