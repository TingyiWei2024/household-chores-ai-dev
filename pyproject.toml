[project]
name = "household-chores"
version = "0.1.0"
description = "A shared household chore management application."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=5.2,<5.3",
]
